---
phase: 01-project-setup
plan: 01-02
type: execute
---

<objective>
Add React Router v6 for tab-based navigation. Create the global shell (TabBar + StatusBar) and empty page stubs for all three screens. Wire up App.tsx to use the router.

Purpose: All subsequent component plans drop into pre-existing route stubs. Shell is visible and correct before any real data.
Output: App renders shell + 3 navigable tabs with empty content areas.
</objective>

<context>
@.planning/BRIEF.md
@docs/superpowers/specs/2026-03-28-arbx-frontend-design.md
@app/src/lib/types.ts
@app/src/lib/api.ts
@app/src/main.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install React Router + create page stubs</name>
  <files>
    app/package.json,
    app/src/pages/SignalsPage.tsx,
    app/src/pages/MarketsPage.tsx,
    app/src/pages/PipelinePage.tsx
  </files>
  <action>
    Install: `cd app && npm install react-router-dom`

    Create `app/src/pages/SignalsPage.tsx`:
    ```tsx
    export default function SignalsPage() {
      return <div className="p-4 text-text-muted font-mono text-xs tracking-widest">SIGNALS — COMING SOON</div>;
    }
    ```

    Create `app/src/pages/MarketsPage.tsx`:
    ```tsx
    export default function MarketsPage() {
      return <div className="p-4 text-text-muted font-mono text-xs tracking-widest">MARKETS — COMING SOON</div>;
    }
    ```

    Create `app/src/pages/PipelinePage.tsx`:
    ```tsx
    export default function PipelinePage() {
      return <div className="p-4 text-text-muted font-mono text-xs tracking-widest">PIPELINE — COMING SOON</div>;
    }
    ```
  </action>
  <verify>cd app && npx tsc --noEmit 2>&1 | head -10</verify>
  <done>Three page files exist, no TS errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build TabBar component</name>
  <files>app/src/components/TabBar.tsx</files>
  <action>
    Create `app/src/components/TabBar.tsx`:

    ```tsx
    import { NavLink } from 'react-router-dom';

    interface TabBarProps {
      candidateCount: number;
      isLive: boolean;
    }

    export default function TabBar({ candidateCount, isLive }: TabBarProps) {
      const tabs = [
        { label: 'SIGNALS', to: '/signals' },
        { label: 'MARKETS', to: '/markets' },
        { label: 'PIPELINE', to: '/pipeline' },
      ];

      return (
        <header className="flex items-stretch h-12 border-b border-border bg-bg shrink-0">
          {/* Wordmark */}
          <div className="flex items-center px-4 border-r border-border">
            <span className="text-orange font-bold tracking-[4px] text-sm">ARBX</span>
          </div>

          {/* Tabs */}
          {tabs.map((tab) => (
            <NavLink
              key={tab.to}
              to={tab.to}
              className={({ isActive }) =>
                `flex items-center px-4 border-r border-border text-xs tracking-widest transition-colors ` +
                (isActive
                  ? 'text-white border-b-2 border-b-orange bg-surface'
                  : 'text-text-muted hover:text-text-secondary')
              }
            >
              {tab.label}
            </NavLink>
          ))}

          {/* Live indicator + count */}
          <div className="ml-auto flex items-center gap-3 px-4">
            <span
              className={`text-xs ${isLive ? 'text-green' : 'text-text-muted'}`}
              style={isLive ? { animation: 'pulse 2s cubic-bezier(0.4,0,0.6,1) infinite' } : {}}
            >
              ● LIVE
            </span>
            {candidateCount > 0 && (
              <span className="text-xs text-orange tracking-widest">{candidateCount} CANDIDATES</span>
            )}
          </div>
        </header>
      );
    }
    ```
  </action>
  <verify>cd app && npx tsc --noEmit 2>&1 | head -10</verify>
  <done>File exists, no TS errors.</done>
</task>

<task type="auto">
  <name>Task 3: Build StatusBar component</name>
  <files>app/src/components/StatusBar.tsx</files>
  <action>
    Create `app/src/components/StatusBar.tsx`:

    ```tsx
    import type { AppConfig } from '../lib/types';

    interface StatusBarProps {
      config: AppConfig | null;
      error: boolean;
    }

    export default function StatusBar({ config, error }: StatusBarProps) {
      const dbColor =
        config?.db_status === 'connected' ? 'text-green' :
        config?.db_status === 'disconnected' ? 'text-amber' : 'text-red';

      const dbLabel =
        config?.db_status === 'connected' ? 'CONNECTED' :
        config?.db_status === 'disconnected' ? 'DISCONNECTED' :
        error ? 'ERROR' : '--';

      return (
        <footer className="flex items-center gap-6 h-7 px-4 border-t border-border bg-bg shrink-0 text-[11px] font-mono tracking-wider">
          <span className="text-text-muted">
            MODEL: <span className="text-text-secondary">{config?.embedding_model ?? '--'}</span>
          </span>
          <span className="text-text-muted">
            THRESHOLD: <span className="text-text-secondary">{config?.similarity_threshold ?? '--'}</span>
          </span>
          <span className="text-text-muted">
            DB: <span className={dbColor}>{dbLabel}</span>
          </span>
          <span className="text-text-muted">
            MARKETS: <span className="text-text-secondary">{config?.markets?.join(' · ') ?? '--'}</span>
          </span>
        </footer>
      );
    }
    ```
  </action>
  <verify>cd app && npx tsc --noEmit 2>&1 | head -10</verify>
  <done>File exists, no TS errors.</done>
</task>

<task type="auto">
  <name>Task 4: Wire App.tsx — router + shell + config fetch</name>
  <files>app/src/App.tsx, app/src/main.tsx</files>
  <action>
    Replace `app/src/main.tsx` with:
    ```tsx
    import { StrictMode } from 'react';
    import { createRoot } from 'react-dom/client';
    import { BrowserRouter } from 'react-router-dom';
    import './index.css';
    import App from './App';

    createRoot(document.getElementById('root')!).render(
      <StrictMode>
        <BrowserRouter>
          <App />
        </BrowserRouter>
      </StrictMode>
    );
    ```

    Replace `app/src/App.tsx` with:
    ```tsx
    import { Routes, Route, Navigate, useEffect, useState } from 'react-router-dom';
    import TabBar from './components/TabBar';
    import StatusBar from './components/StatusBar';
    import SignalsPage from './pages/SignalsPage';
    import MarketsPage from './pages/MarketsPage';
    import PipelinePage from './pages/PipelinePage';
    import { api } from './lib/api';
    import type { AppConfig } from './lib/types';

    export default function App() {
      const [config, setConfig] = useState<AppConfig | null>(null);
      const [configError, setConfigError] = useState(false);
      const [candidateCount, setCandidateCount] = useState(0);

      useEffect(() => {
        api.getConfig()
          .then(setConfig)
          .catch(() => setConfigError(true));

        api.getCandidates()
          .then((c) => setCandidateCount(c.length))
          .catch(() => {});
      }, []);

      return (
        <div className="flex flex-col h-screen bg-bg">
          <TabBar candidateCount={candidateCount} isLive={config?.db_status === 'connected'} />
          <main className="flex-1 overflow-hidden">
            <Routes>
              <Route path="/" element={<Navigate to="/signals" replace />} />
              <Route path="/signals" element={<SignalsPage />} />
              <Route path="/markets" element={<MarketsPage />} />
              <Route path="/pipeline" element={<PipelinePage />} />
            </Routes>
          </main>
          <StatusBar config={config} error={configError} />
        </div>
      );
    }
    ```
  </action>
  <verify>cd app && npm run build 2>&1 | tail -10</verify>
  <done>Build succeeds. `npm run dev` starts and the browser shows ARBX tab bar + status bar with three navigable tabs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - React Router installed
    - TabBar: ARBX wordmark + SIGNALS/MARKETS/PIPELINE tabs (active tab orange underline) + ● LIVE indicator
    - StatusBar: MODEL / THRESHOLD / DB / MARKETS fields at bottom
    - All tabs navigate to stub pages
    - App fetches /api/config and /api/candidates on mount (will show errors in console until FastAPI is running — that's expected)
  </what-built>
  <how-to-verify>
    1. `cd app && npm run dev`
    2. Open http://localhost:5173
    3. Confirm dark navy background, ARBX wordmark in orange on the left
    4. Click SIGNALS / MARKETS / PIPELINE — active tab gets orange underline
    5. Bottom status bar is visible with muted field labels
    6. Console will show fetch errors (FastAPI not running yet) — acceptable
  </how-to-verify>
  <resume-signal>Type "shell looks good" to continue to FastAPI backend, or describe any visual issues.</resume-signal>
</task>

</tasks>

<verification>
- `cd app && npm run build` exits 0
- `npx tsc --noEmit` exits 0
- Three routes render without crashing
</verification>

<success_criteria>
- React Router v6 installed
- TabBar + StatusBar components exist and render
- App.tsx wires router + shell + config fetch
- Build passes
</success_criteria>

<output>
SUMMARY should include: packages installed, any deviations, build status.
</output>
