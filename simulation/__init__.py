"""
ArbIt Simulation — historical replay and backtesting environment.

Quick start:
    from simulation.run_backtest import run_backtest
    from simulation.config import SimulationConfig
    from simulation.models import RealismMode

    result = run_backtest(SimulationConfig(realism_mode=RealismMode.REALISTIC))
"""
